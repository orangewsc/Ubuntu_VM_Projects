本目录下脚本为批量全速压缩文件作用。


1. run_install_xz.sh 
作用： 升级xz版本为v5.2.2，以支持多线程压缩。升级只需运行一次。如果xz的版本已经是v5.2.2+，则不需运行次脚本。操作目录为当前目录。
命令： 
./run_install_xz.sh
（中间会提示输入密码）

2. run_tar_logs.sh 
作用：批量打包当前目录下的.log文件到.tar.xz格式。如果xz版本在v5.2.2+，则会占用所有CPU全速压缩；反之，则占用单个CPU压缩。操作时，将此脚本拷贝到.log文件所在目录再运行。
命令： 
./run_tar_logs.sh 
